/* Phase 5 — schema migration with backup to .AMC/backups/, verify, restore
   on failure. settings.json carries schemaVersion from its very first write
   (Phase 2) so there is something to migrate from. Not part of Phase 1. */
export {};
