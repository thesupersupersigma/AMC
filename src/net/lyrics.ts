/* Phase 4 — lyrics sources: .AMC/lyrics/, sibling .lrc, LYRICS tags, LRCLIB
   through /api/lrclib. Fetch once, write to the sidecar, offline thereafter.
   Not part of the Phase 1 port. */
export {};
