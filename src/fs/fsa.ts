/* Phase 2 — File System Access backend: showDirectoryPicker({ mode:
   'readwrite' }), handles persisted in IndexedDB, queryPermission on launch,
   requestPermission only from a user gesture. Not part of the Phase 1 port. */
export {};
